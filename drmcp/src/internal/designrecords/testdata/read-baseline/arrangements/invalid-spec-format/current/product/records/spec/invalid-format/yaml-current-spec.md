---
design_record:
  id: spec:product.invalid_format.yaml_current_spec
  status: accepted
  parent: root
---
# Overview: YAML front matter current spec fixture

## What this is

Intentionally uses YAML front matter instead of H1-adjacent current spec metadata.
