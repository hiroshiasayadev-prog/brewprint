# DRMCP-TASK-MCP-016-03: Decide DRMCP application-architecture graph and boundaries

- **id**: DRMCP-TASK-MCP-016-03
- **status**: done
- **date**: 2026-07-03
- **work_item**: DRMCP-WORK-MCP-016
- **task_type**: decision
- **estimate**: 2.0d
- **depends_on**:
  - DRMCP-TASK-MCP-016-02
- **outputs**:
  - DRMCP-TASK-MCP-016-03

## Goal

Decide a coherent DRMCP whole-application architecture graph and responsibility boundaries from the accepted Work Item contract and T02 Evidence.

## Work

- Consume the complete use-case and architecture-input inventory from T02.
- Build and maintain one resumable decision inventory.
- Decide the application-level component partition needed to realize the observed use cases.
- Decide each component's owned and excluded responsibilities.
- Decide allowed dependency direction and forbidden dependencies.
- Decide major runtime collaboration and stage ownership across the identified use-case families.
- Decide startup, composition, runtime-state, and resource-lifecycle ownership.
- Decide which downstream changes remain local contract or detailed-design refinements and which changes must return to architecture decision work.
- Identify expected canonical architecture views and provisional ADR routing without authoring ADRs or Specifications.
- Ask exactly one unresolved user judgment at a time and persist each answer before advancing.

### Decision inventory

| ID | topic | status | depends on | decision summary | reason / evidence | canonical target | ADR route |
|---|---|---|---|---|---|---|---|
| D-001 | Whole-application boundary and operation-family scope | `decided` | none | Include all eleven current public operations and their shared application capabilities. Exclude retired tombstones, existing brewprint MCP internals, model-provider behavior, and host-level concerns not specified as DRMCP responsibilities. Treat PRODUCT standards, configured repositories, guidance package, proposal/cache storage, repository writing, and MCP transport as explicit external dependencies or boundaries consumed by DRMCP. | The accepted scope covers every current public operation without inventing unsupported dependencies or reviving retired behavior. A dependency boundary does not imply a separate process; concrete deployment and storage choices remain downstream. | application architecture overview / boundary view | candidate |
| D-002 | Authoring-family architecture boundary under incomplete specifications | `decided` | D-001 | Do not derive concrete authoring components, state ownership, write semantics, or transaction behavior from the current stale and incomplete authoring Specifications. Preserve only a minimum architecture seam: MCP routing remains separate from dedicated application use cases so later authoring capabilities can be added without collapsing protocol handling into application policy. Treat current authoring operation names and broad intent as scope signals, not normative internal design. | The user judged the current authoring Specifications too incomplete and stale to support whole internal architecture decisions. Fixing only the routing-versus-use-case separation avoids contaminating the architecture while preserving an extension point. T02 G-03, G-04, G-05, G-07, G-08, G-14, G-15, G-17. | application boundary and dependency views; later authoring contract work | candidate |
| D-003 | Use-case family and actor classification | `decided` | D-001, D-002 | Classify the current baseline into three active use-case families—read/query (`list_records`, `get_records`, `resolve_reference`), validation (`validate_records`), and guidance (`list_authoring_guides`, `get_authoring_guidance`)—plus one deferred authoring extension family containing the five proposal/write operations. Classify MCP client/host as the inbound actor; configured repositories, PRODUCT standards, guidance package, and runtime configuration as external providers; retired operations, model provider, existing brewprint MCP internals, and unspecified host policy as excluded actors. | The accepted classification makes the current architecture scope visible before component design. The six active operations require concrete architecture now; authoring retains only a routing-to-use-case seam. | application boundary and use-case classification view | candidate |
| D-004 | Snapshot, freshness, and persisted-state validation baseline | `decided` | D-003, D-017 | Each Read/Validation request builds one fresh immutable current-record snapshot and, when configured, one separate legacy lookup snapshot. The request uses that state from start to finish and discards it afterward. Guidance requests use their own source access and are not forced through the record snapshot. No process-wide mutable index, watcher, background refresh, or shared record cache is part of the baseline. If a trustworthy complete snapshot cannot be built, the operation fails instead of returning partial normal data. | The accepted request-scoped model keeps current and legacy state separate, makes filesystem changes visible on the next request, and avoids hidden mutable server state. | lifecycle and runtime-collaboration views | candidate |
| D-005 | Minimum stable application-level component graph | `decided` | D-001, D-002, D-003, D-004, D-017 | Use six application-level components—(1) Composition/Lifecycle, (2) MCP Inbound Adapter, (3) Application Use Cases, (4) Record Domain / Logical Tree, (5) Guidance Domain, and (6) Infrastructure I/O Adapters. Application Use Cases own operation orchestration and may share application-level helpers, but do not own record semantics or concrete I/O. Record Domain / Logical Tree owns the transport- and filesystem-independent record model, logical tree/index, identity, resolution, and validation behavior. Guidance Domain owns guide catalog/content semantics independent of concrete storage. Infrastructure I/O Adapters own concrete enumeration/read operations and implement narrow inward-owned source ports. Composition wires adapters to use cases; MCP maps protocol requests to use cases. Deferred Authoring adds no concrete components now beyond preserving the same adapter-to-use-case seam. | The accepted graph follows Clean Architecture-style use case, domain/logical-tree, and infrastructure/I/O boundaries while remaining at application level rather than package/interface detail. Shared domain and infrastructure capabilities may serve multiple use cases. | component view | candidate |
| D-006 | Component responsibility ownership and exclusions | `decided` | D-005 | Keep each accepted component broad enough to avoid layer proliferation. Composition/Lifecycle owns only construction, wiring, startup, and shutdown. MCP Inbound Adapter owns protocol mapping only. Application Use Cases own operation sequencing, request-scoped snapshot orchestration, and operation-specific result policy. Record Domain / Logical Tree owns pure record semantics and immutable logical structures. Guidance Domain is an explicit architectural placement and dependency boundary for guide-specific logic; whether its implementation is only simple values/functions or a richer module is downstream detail, not an architecture decision. Infrastructure I/O Adapters own concrete external access. Do not create separate application-level components for parser, resolver, validator, snapshot builder, index, or each filesystem source unless later contract work proves an independent lifecycle, owner, substitution boundary, or cross-component contract. | The accepted rule fixes placement and dependency ownership without requiring one package, type, or runtime object per architecture box. Guidance remains an explicit domain-side placement rather than an optional architecture component. This limits overengineering while preserving the chosen boundary. | component and boundary views | candidate |
| D-007 | Dependency direction and forbidden dependencies | `decided` | D-005, D-006 | Dependencies point inward. MCP Inbound Adapter depends on Application Use Cases, not vice versa. Application Use Cases depend on Record/Guidance Domain and on application-owned source ports. Infrastructure I/O Adapters implement those ports and may depend inward on their contracts, while Domain depends on neither Application, MCP, configuration, nor filesystem. Composition/Lifecycle may depend on every concrete component solely to construct and wire them. Forbid use cases from importing concrete filesystem adapters, Domain from performing I/O, adapters from calling public MCP tools, and lateral adapter-to-adapter orchestration. | The accepted direction preserves Clean Architecture boundaries while allowing runtime calls to flow outward through inward-owned ports. T02 AI-04; CC-07–CC-09, CC-16, CC-17. | dependency view | candidate |
| D-008 | Read and validation runtime collaboration | `decided` | D-004, D-005, D-006, D-007 | Split the Application layer internally into operation-specific use cases and shared application orchestration. Operation-specific use cases own public-operation policy and result semantics for list, get, resolve, and validate. Shared application orchestration owns reusable request-scoped source loading and logical-snapshot assembly flow used by multiple operations. Public use cases do not call one another; they depend on shared application orchestration plus Record Domain capabilities. The shared orchestration is not a new public use case and is not automatically a separate top-level architecture component. | The accepted split preserves operation boundaries while allowing stable application-level reuse. | runtime-collaboration and application-internal responsibility views | candidate |
| D-009 | Current-versus-legacy source capability boundary | `decided` | D-005, D-006, D-007 | Keep Current Records and Legacy Archive as separate source capabilities, separate source ports, and separate logical state inside the request context. Current Records produce the active logical tree/index and validation subjects. Legacy Archive produces only exact issued-ID lookup state used by retrieval, resolution, and relation validation. Do not merge both into one repository abstraction or one generic index. Operation-specific use cases decide whether legacy state is needed; shared orchestration may load both through distinct ports. | The accepted split preserves the different semantics, visibility, validation role, and read-only compatibility role of Current and Legacy data. | component, dependency, and runtime views | candidate |
| D-010 | PRODUCT standards responsibility placement | `decided` | D-002, D-005, D-006, D-007 | This adds no new application-level component. PRODUCT remains the external semantic authority. Record Domain / Logical Tree owns the domain-side rule contracts/policies and applies them when constructing or validating logical state. Composition/Lifecycle selects and injects the concrete rule implementation or catalog at startup. Infrastructure I/O Adapters own concrete standards-file loading only when the chosen downstream design uses external files; compiled or generated rules need no standards I/O adapter. Application Use Cases invoke Domain behavior but do not interpret PRODUCT standards. MCP Inbound Adapter and Guidance Domain have no responsibility here. Stale DRMCP-local field catalogs and old authoring schemas are not authority. | The accepted placement maps authority, application, startup wiring, and optional I/O onto the existing component graph without inventing a new rule component. | component responsibility, external boundary, and dependency views | candidate |
| D-011 | Guidance source and runtime ownership | `decided` | D-005, D-006, D-007 | Keep Guidance deliberately minimal. The authoritative guide source is the read-only directory `product/records/spec/design-records/authoring-standards/`; the current DRMCP guidance Specifications that name `docs/guides/*.md` are stale and require downstream correction. `list_authoring_guides` and `get_authoring_guidance` remain operation-specific Application Use Cases. Infrastructure I/O Adapters own directory enumeration and file reads through a Guidance Source port. Guidance Domain owns only the pure path-independent rules required by the operation contract: guide identity derivation, first-H1 title, `## Abstract` extraction, exact ID lookup semantics, ASCII ordering, and guide projection values. Guidance does not use the Record Logical Tree or record snapshot. No preload lifecycle, cache, catalog store, background refresh, or separate stateful subsystem is part of the architecture baseline. | The accepted placement reflects the actual repository source identified by the user and preserves the minimal filesystem-backed retrieval surface. The stale path in current DRMCP Specifications is a downstream Specification correction, not architecture authority. | component and runtime-collaboration views; downstream Specification correction | candidate |
| D-012 | Proposal and body-cache state ownership | `deferred` | D-002, D-005, D-006, D-007 | Do not decide concrete proposal-store, body-cache, retention, TTL, cleanup, restart, concurrency, or persistence ownership from the stale authoring Specifications. The introduction of request-spanning mutable proposal state must return to application-architecture decision work. Future design should begin from a non-binding responsibility example: Domain may own proposal identity, states, transitions, acceptability rules, and proposal invariants; Application may own propose/get/accept/discard orchestration and ordering of Domain and persistence operations; Infrastructure may own proposal/body storage and repository writes; Composition/Lifecycle may own store construction, wiring, cleanup, and shutdown. This example prevents dumping all retained-state concerns into Domain but is not a binding component contract. | D-002 excludes concrete authoring internals. The user accepted deferral and requested the cross-layer responsibility example be preserved because proposal semantics are Domain-centered while retained-state mechanics span Application, Infrastructure, and Composition. | future state and lifecycle views; authoring architecture return | none now |
| D-013 | Write transaction and post-write validation ownership | `deferred` | D-002, D-004, D-012 | Defer concrete write-transaction and post-write validation architecture for the same reason as D-012. Preserve only the requirement that accepting a proposal remains an Application Use Case behind the MCP seam, and require architecture return before deciding write atomicity, rollback, affected-set validation, repository mutation, or consistency between retained proposal state and filesystem state. Do not infer those responsibilities from the current stale authoring contracts. | T02 AI-12; UC-A04; LF-10, LF-11; G-08, G-14, G-17; D-002 and D-012. | future runtime-collaboration and lifecycle views; authoring architecture return | none now |
| D-014 | Failure ownership and semantic-result versus execution-error boundary | `decided` | D-008, D-011, D-012, D-013 | Use three failure classes for the active Read/Validation and Guidance baseline. (1) Request rejection: MCP Inbound Adapter owns protocol/schema decoding failures; Application Use Cases own operation-specific request invalidity such as unsupported selectors or invalid scope combinations. No normal operation response is returned. (2) Expected semantic outcome: Record/Guidance Domain produces modeled states or findings such as unresolved, unsupported, duplicate conflict, invalid record/relation, unreadable indexed target, zero match, or guide-not-found meaning; Application Use Cases decide the operation-specific projection as normal result, warning/diagnostic, partial success, or declared operation error. (3) Execution failure: Infrastructure reports concrete access failures, and Application aborts when mandatory configuration/source state, a complete trustworthy snapshot, or a required source-backed diagnostic location cannot be constructed. MCP Adapter only encodes the selected result/error and does not reclassify it. The governing test is whether the use case can still produce the complete trustworthy result promised by its contract. | The accepted model preserves semantic invalidity as trustworthy operation output while preventing incomplete or misleading normal responses. Authoring failure behavior remains deferred. | boundary and runtime views | candidate |
| D-015 | Downstream-local refinement versus return-to-architecture rule | `decided` | D-005 through D-014 | Downstream contract or detailed-design work may choose package layout, concrete interfaces and data types, parser/validator algorithms, adapter APIs, filesystem libraries, rule encoding, snapshot helper structure, caching that does not alter ownership or lifetime semantics, and error-code representation within the accepted component/dependency boundaries. Work must return to application architecture when it would add/remove a top-level component, reverse or bypass an accepted dependency, move a responsibility across component boundaries, introduce request-spanning mutable state or a new resource lifecycle, change source authority or current/legacy separation, require cross-use-case coordination not covered by shared application orchestration, change trustworthy-result/failure semantics, or make an external provider/process part of runtime behavior. | The accepted guardrail protects the architecture from accidental erosion while keeping implementation and module-contract work local. | downstream boundary view | candidate |
| D-016 | Canonical architecture views and ADR partition candidates | `decided` | D-001 through D-015, D-017 | Project the accepted decisions into four canonical application-architecture views rather than one monolithic document: (1) application-boundary-and-component-view covering scope, actors, the six components, and deferred authoring seam; (2) dependency-and-responsibility-view covering inward dependencies, owned/excluded responsibilities, standards placement, Guidance placement, and forbidden edges; (3) runtime-and-state-view covering request-scoped snapshots, Current/Legacy separation, Application internal collaboration, lifecycle/configuration, and deferred mutable authoring state; and (4) failure-and-evolution-view covering request/semantic/execution failure ownership plus downstream-local versus architecture-return rules. Provisional ADR routing is minimal: one ADR for the six-component application architecture and dependency direction; one ADR for request-scoped state/current-versus-legacy lifecycle; one ADR for authoring architecture deferral and required return triggers. Guidance source-path correction and concrete standards/operation contracts are Specification corrections, not separate architecture ADRs. Exact filenames, Specification topology, and final ADR count remain downstream graph/ADR-routing work. | The accepted four-view partition keeps distinct concerns reviewable without turning every decision into a separate document. Three provisional ADR themes capture durable alternatives and consequences; local path/spec corrections do not warrant architecture ADRs. | application architecture Specification set and provisional ADR routing inputs | candidate |
| D-017 | Configuration and server lifecycle ownership | `decided` | D-003 | One composition/lifecycle owner loads and validates runtime configuration at startup, constructs and wires the active application, starts the MCP server, and shuts resources down in a defined order. Active use cases receive validated runtime dependencies and do not reload configuration, construct adapters, or own server shutdown. Runtime configuration is immutable for the baseline server lifetime. | The accepted split keeps server lifecycle and dependency construction outside the six active use cases. | lifecycle view | candidate |

### Status definitions

- `open`: known judgment not yet selected.
- `in_discussion`: the one current user judgment.
- `decided`: explicit accepted outcome persisted.
- `blocked`: cannot proceed until a named dependency or authority is available.
- `deferred`: explicitly routed outside this Work Item.
- `superseded`: replaced by a later decision item.

### Current cursor

- Decision: none
- Loop state: `complete`
- Next action: Hand the accepted decision set to downstream graph coordination, ADR routing, and Specification authoring.

## Done condition

- Every architecture decision required by the W016 Done conditions and T02 Evidence is `decided`, `deferred`, or validly `blocked`.
- The selected component graph covers the inventoried use cases without collapsing into exact module contracts or implementation detail.
- Responsibility ownership, dependency direction, runtime collaboration, lifecycle ownership, and downstream return rules are coherent with one another.
- Expected canonical architecture views and provisional ADR routes are explicit.
- No unresolved architecture judgment remains hidden in downstream authoring.

## Verification

- Confirm T02 Evidence was consumed and every relevant use-case family is covered by the decision ledger.
- Confirm at most one decision item is `in_discussion`.
- Confirm every explicit user answer was persisted before the cursor advanced.
- Confirm exact packages, interfaces, types, functions, algorithms, and implementation Tasks were not decided.
- Confirm ADR and Specification authoring did not occur.

## Evidence

- T02 completed and produced a traceable inventory of eleven current public operations, thirteen lifecycle flows, external boundaries, state/resources, eighteen cross-cutting constraints, nineteen gaps/conflicts, and AI-01 through AI-16.
- The formal decision ledger above consumes those inputs and preserves their dependency order.
- D-001 fixes the whole-application scope around all eleven current public operations and explicit external dependency boundaries.
- D-002 excludes concrete authoring internals from the current architecture baseline and preserves only the routing-versus-use-case seam for future addition.
- D-003 fixes the active, deferred, external, and excluded use-case/actor classification.
- D-017 fixes startup configuration, dependency wiring, server run, and ordered shutdown under one composition/lifecycle owner.
- D-004 fixes one fresh immutable current snapshot and one separate legacy lookup snapshot per Read/Validation request, with Guidance outside that state model.
- D-005 fixes the six-component Clean Architecture graph.
- D-006 fixes responsibility placement, keeps Guidance Domain as an explicit architectural boundary, and leaves implementation thickness to downstream design.
- D-007 fixes inward dependency direction and the forbidden dependency edges.
- D-008 fixes the internal Application split between operation-specific use cases and shared application orchestration.
- D-009 fixes Current Records and Legacy Archive as separate source capabilities and separate logical states.
- D-010 fixes standards authority and responsibility placement across PRODUCT, Record Domain, Composition, and optional Infrastructure I/O.
- D-011 fixes Guidance as a minimal read-only source over `product/records/spec/design-records/authoring-standards/` and records the current `docs/guides/*.md` DRMCP Specification path as stale.
- D-012 defers concrete proposal/body-cache ownership, records request-spanning mutable state as an architecture-return trigger, and preserves a non-binding cross-layer responsibility example.
- D-013 defers write transaction and post-write validation ownership while preserving the MCP-to-Application seam and architecture-return requirement.
- D-014 fixes the three-class request-rejection, expected-semantic-outcome, and execution-failure model. Authoring failures remain deferred.
- D-015 fixes the guardrail between local downstream refinement and architecture return.
- D-016 fixes the four canonical architecture views and three provisional ADR themes.
- All decision items are now `decided` or explicitly `deferred`; no item remains `open`, `blocked`, or `in_discussion`.
- The Work Item's architecture judgment is complete and ready for downstream graph coordination, ADR routing, canonical authoring, and independent review.
- This Task owns architecture judgment only; downstream graph coordination, ADR routing, canonical authoring, review, and closure remain separate responsibilities.
