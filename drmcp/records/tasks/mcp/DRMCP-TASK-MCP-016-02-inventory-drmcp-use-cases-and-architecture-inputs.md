# DRMCP-TASK-MCP-016-02: Inventory DRMCP use cases and architecture inputs

- **id**: DRMCP-TASK-MCP-016-02
- **status**: done
- **date**: 2026-07-03
- **work_item**: DRMCP-WORK-MCP-016
- **task_type**: investigation
- **estimate**: 1.0d
- **depends_on**:
  - DRMCP-TASK-MCP-016-01
- **outputs**:
  - DRMCP-TASK-MCP-016-02

## Goal

Produce a repository-grounded inventory of DRMCP application use cases, lifecycle flows, external boundaries, and architecture-relevant constraints from current canonical specifications.

## Work

- Read the current DRMCP Specification trees under `drmcp/records/spec/design-records-mcp/` and `drmcp/records/spec/implementation/`.
- Inventory every application-level user operation, internal application workflow, startup or shutdown flow, and persistent or runtime-state lifecycle expressed by those Specifications.
- For each use case or lifecycle flow, record:
  - initiating actor or boundary;
  - input and preconditions;
  - required application outcome;
  - application-owned orchestration or policy;
  - source, state, resource, or external system touched;
  - success, partial-success, structural-failure, and execution-failure distinctions when specified;
  - exact source Specification refs.
- Record architecture-relevant cross-cutting constraints and existing ownership claims separately from use cases.
- Identify missing coverage, contradictions, stale read-runtime assumptions, and questions that require T03 judgment.
- Record the complete bounded research result directly in this Task's `## Evidence` under the accepted lightweight Investigation exception.
- Do not select components, architecture style, dependency direction, or canonical architecture views.

## Done condition

- Every current DRMCP application-level operation and lifecycle flow found in the scoped Specifications is represented or explicitly classified as out of architecture scope.
- Each inventory entry traces to current Specification Evidence.
- External boundaries, state and resource ownership inputs, failure distinctions, and cross-cutting constraints are explicit.
- Gaps and unresolved architecture questions are separated from observed facts.
- The Evidence is sufficient for T03 to derive and decide the DRMCP application architecture without reconstructing the inventory from chat.

## Verification

- Confirm both current DRMCP Specification trees were covered.
- Confirm use-case entries distinguish observed facts from candidate architecture implications.
- Confirm no component partition, dependency rule, ADR route, or canonical architecture content was adopted.
- Confirm T03 is named as the downstream consumer and conclusion owner.
- Confirm no separate Investigation record was created.

## Evidence

### Scope and sources

- The user explicitly accepted direct Task Evidence instead of a separate Investigation record because this bounded work is an architecture-input inventory rather than a durable standalone research result.
- `DRMCP-TASK-MCP-016-03` is the separate downstream decision Task that consumes and concludes this Evidence.
- DRMCP is non-operational, so Design Records MCP could not be used. Filesystem fallback was used for all reads and for this Task update.
- Startup authority read first: repository-root `prompt_chappy.md`.
- Workflow records read: `DRMCP-WORK-MCP-016`, `DRMCP-TASK-MCP-016-01`, this Task, `DRMCP-TASK-MCP-016-03`, and `DRMCP-REQ-MCP-005`.
- Authoring authority read: `spec:product.design_records.authoring_standards.task_authoring` at `product/records/spec/design-records/authoring-standards/task-authoring.md`.
- Scoped Specification trees were fully enumerated and read: 30 Markdown files under `drmcp/records/spec/design-records-mcp/` and one Markdown file under `drmcp/records/spec/implementation/`.
- Design Records MCP sources covered:
  - root: `spec:drmcp.design_records_mcp.overview`, `mvp_scope`, `namespace_scanning`, `resolver`, and `responsibility_boundary`;
  - schema: `schema.overview`, `record_source`, `metadata_grammar`, `fields`, `id_normalization`, `discovery`, `record_model`, `diagnostics`, `authoring_guidance_source`, and `authoring_transaction_schema`;
  - tools: `tools.overview`, the four read-runtime operations, two authoring-guidance operations, five authoring-transaction operations, shared authoring-transaction model, and the two retired-operation tombstones;
  - implementation: `spec:drmcp.implementation`.
- In the inventories below, **Observed** means directly stated by a scoped Specification, **Gap/conflict** means a comparison result between scoped Specifications or the required Task-authoring authority, and **T03 question** is an architecture input only. No candidate is adopted here.

### Use-case inventory

| local ID | use case | initiating actor / boundary | input and precondition | required application outcome and owned policy | touched source, state, resource, or external system | success / partial success / structural failure / execution failure | exact source | T03 question |
|---|---|---|---|---|---|---|---|---|
| `UC-R01` | List current records | MCP client through MCP transport | Exact configured `app_namespace`, supported sequential `kind`, canonical `domain`; optional accepted status, order, and limit | Filter active-index entries, order by canonical ref, limit, project compact fields, and emit operation warnings | Validated configuration, fresh current active index, current source provenance for warning locations | Success: compact records and `has_more`; partial: invalid-but-addressable records remain visible with missing compact fields as `null` plus warnings; structural: `invalid_request`; execution: untrustworthy snapshot or required warning location unavailable produces no normal response | `spec:drmcp.design_records_mcp.tools.list_records`; `spec:drmcp.design_records_mcp.namespace_scanning`; `spec:drmcp.implementation` | Whether listing remains a dedicated application capability or shares a broader read/query component without introducing the prohibited generic router. |
| `UC-R02` | Exact record retrieval | MCP client through MCP transport | One to 20 exact string refs and request-wide `include_body`; current, current-spec, and accepted legacy grammars are classified exactly once | Deduplicate exact strings, classify each first occurrence, query only its selected current or legacy source, preserve first-occurrence order, and project successful records plus warnings | Fresh current active index, separate legacy lookup map, source Markdown, parsed metadata/headings/body, diagnostic locations | Success: one or more current or legacy records; partial: failed refs produce warnings while other records remain; structural: invalid request shape; execution: snapshot/read/location failure prevents a trustworthy normal response | `spec:drmcp.design_records_mcp.tools.get_records`; `spec:drmcp.design_records_mcp.schema.diagnostics`; `spec:drmcp.implementation` | Where exact-input classification and source-content projection belong, and how legacy source reads share infrastructure without merging current and legacy state. |
| `UC-R03` | Resolve one reference | MCP client through MCP transport | One string `ref`; request shape valid; exact current-first evaluation | Evaluate current grammar and active index first, then accepted legacy grammar and configured legacy lookup; return one target at most with fixed `resolved` / `unresolved` / `unsupported` status and cause diagnostics | Fresh current active index, separate legacy lookup map, current parsed record fields, legacy source readability, diagnostics | Success: current or legacy target; normal negative result: unresolved or unsupported with diagnostics; structural: `invalid_request`; execution: required conflict/unreadable-source location or trustworthy snapshot unavailable | `spec:drmcp.design_records_mcp.resolver`; `spec:drmcp.design_records_mcp.tools.resolve_reference`; `spec:drmcp.implementation` | How resolver policy remains distinct from `get_records` classification while reusing grammar, index, legacy lookup, and diagnostic capabilities. |
| `UC-R04` | Validate current records | MCP client through MCP transport | Empty request, exact configured app selector, or exact current ref selector; complete mandatory configuration and index state must be buildable | Select retained current validation subjects, run ordered source/identity/document/relation/topics checks, aggregate and suppress duplicate findings, deterministically order them, and compute `ok` | Fresh current snapshot, all retained source and parsed state, active index, duplicate groups, separate legacy lookup state for relation targets, PRODUCT semantic authorities, diagnostic locations | Success: normal wrapper with findings and summary; no partial normal response from incomplete roots/index/locations; structural: invalid selector/request; execution: mandatory configuration/index/location unavailable; record invalidity remains normal diagnostic data | `spec:drmcp.design_records_mcp.tools.validate_records`; `spec:drmcp.design_records_mcp.schema.diagnostics`; `spec:drmcp.implementation` | Ownership split between rule catalog, validator execution, graph validation, finding aggregation, and public projection across standalone and proposal-local validation. |
| `UC-G01` | List authoring guides | MCP client through MCP transport | Empty request; guide source is `docs/guides/*.md` | Discover guides, derive ID from filename stem, extract first H1 and `## Abstract`, sort by ASCII ID, and hide source paths | Guidance filesystem package, guide Markdown, parsed title and abstract | Success: ordered guide catalog; structural: unknown fields may be `invalid_request`; missing directory, unreadable files, malformed guides, and partial catalog behavior are unspecified | `spec:drmcp.design_records_mcp.schema.authoring_guidance_source`; `spec:drmcp.design_records_mcp.tools.list_authoring_guides` | Whether guidance is a startup-loaded package, request-scoped source, or separate immutable catalog, and who owns malformed/unreadable guide failure. |
| `UC-G02` | Get authoring guidance | MCP client through MCP transport | Exact guide ID | Resolve guide file internally from ID and return ID, first-H1 title, and verbatim Markdown content without path | Guidance filesystem package and guide Markdown | Success: complete guide; structural/normal error: `guide_not_found`; unreadable or malformed guide execution behavior is unspecified | `spec:drmcp.design_records_mcp.schema.authoring_guidance_source`; `spec:drmcp.design_records_mcp.tools.get_authoring_guidance` | Whether guide lookup shares source-reading ports with Design Records while remaining outside the record/index model. |
| `UC-A01` | Propose record creation | MCP client through MCP transport | Supported kind, exact ID or `new` placeholder, domain/parent context when required, title, structured fields, optional section-only body or body-cache ID, reciprocal mode, and diff mode | Resolve final ID/path, render H1 and metadata, combine section body, optionally include required reciprocal workflow updates, build candidate state and diff, run affected-set validation, retain proposal, and write no files | Current record index, PRODUCT authoring rules, target path resolver, candidate files, reciprocal records, body cache, proposal store, diff generator, validation capability | Success: retained proposal; partial/retry: submitted body may be cached after preparation failure; normal no-write diagnostics include sequence-gap and included reciprocal updates; structural: invalid request/body/cache; execution: proposal preparation or required location failure creates no proposal and writes nothing | `spec:drmcp.design_records_mcp.tools.propose_record_create`; `spec:drmcp.design_records_mcp.tools.authoring_transaction_model`; `spec:drmcp.design_records_mcp.schema.authoring_transaction_schema` | Complete current-format create ownership, placement and allocation authority, candidate-state construction, reciprocal-update transaction boundary, and proposal-store ownership are unresolved and conflict with stale V01 examples. |
| `UC-A02` | Propose record update | MCP client through MCP transport | Existing exact record ID and kind; exactly one update or non-empty operations list; metadata or section selectors; body source when required | Resolve target, apply deterministic metadata/section operations, normalize heading-safe section body, detect conflicts and no-op, validate final affected record state, create one retained proposal, and write no files | Current source file, parsed metadata/headings, body cache, proposal store, diff generator, candidate validation state | Success: retained proposal; partial/normal alternative: no-op returns no proposal with info diagnostic; fallback may canonicalize case-only workflow headings; structural: invalid request/body/selector/conflicting operations; execution: preparation/location failure creates no proposal | `spec:drmcp.design_records_mcp.tools.propose_record_update`; `spec:drmcp.design_records_mcp.tools.authoring_transaction_model`; `spec:drmcp.design_records_mcp.schema.authoring_transaction_schema` | Whether authoring owns a Markdown editor/renderer component, how update operations consume current metadata grammar, and how stale YAML/V01 behavior is removed before architecture projection. |
| `UC-A03` | Retrieve retained proposal | MCP client through MCP transport | Exact proposal ID within retention | Load retained proposal and return lifecycle state plus full patch-equivalent diff | Proposal store, retained proposal/diff/base state | Success: proposal detail; normal errors: not found or expired; storage read/corruption behavior is unspecified | `spec:drmcp.design_records_mcp.tools.get_proposed_write`; `spec:drmcp.design_records_mcp.tools.authoring_transaction_model` | Retained-state persistence and failure ownership across process restart, corruption, expiry, and concurrent access. |
| `UC-A04` | Accept proposed write | MCP client through MCP transport | Exact proposal ID; retained proposal must be proposed, unexpired, unstale, target-stable, collision-free, follow-up-complete, validation-clean, and have constructible affected paths/locations | Recheck proposal and repository state, begin no write until all preconditions pass, write affected files, report actual written files, preserve `written: true` after any post-write failure, and provide validation/repair result without automatic rollback | Proposal store, current repository/index state, filesystem writer, affected files, validation capability, repair guidance, proposal lifecycle state | Success: accepted and files written; normal no-write result: unknown/expired/discarded/already accepted/stale/changed/collision/follow-up/validation/location failure; post-write failure must still report actual writes; atomicity, crash recovery, and partial-write semantics are unspecified | `spec:drmcp.design_records_mcp.tools.accept_proposed_write`; `spec:drmcp.design_records_mcp.tools.authoring_transaction_model` | Write coordinator, file-transaction boundary, locking, crash consistency, proposal-state transition, and post-write validation ownership. |
| `UC-A05` | Discard proposed write | MCP client through MCP transport | Exact proposal ID; proposal not unknown or expired | Transition retained proposal to discarded and prevent later acceptance without repository writes | Proposal store and proposal lifecycle state | Success: discarded; normal failure: not found/expired; accepted proposal is not undone; concurrent accept/discard ordering is unspecified | `spec:drmcp.design_records_mcp.tools.discard_proposed_write`; `spec:drmcp.design_records_mcp.tools.authoring_transaction_model` | Which state owner serializes proposal transitions and defines race outcomes. |

Out-of-operation classification:

| local ID | observed classification | evidence | architecture input |
|---|---|---|---|
| `UC-X01` | `get_record` is retired, absent from the public catalog, and has no compatibility alias. | `spec:drmcp.design_records_mcp.tools.get_record`; `spec:drmcp.design_records_mcp.tools.overview`; `spec:drmcp.design_records_mcp.mvp_scope` | No application use case or adapter route should be inferred from the presence of the tombstone Specification. |
| `UC-X02` | `suggest_next_record` is retired; allocation is assigned to authoring placeholders. | `spec:drmcp.design_records_mcp.tools.suggest_next_record`; `spec:drmcp.design_records_mcp.tools.overview` | Sequential allocation is an authoring concern, not a standalone public operation. |

### Lifecycle-flow inventory

| local ID | lifecycle flow | observed sequence and ownership claim | state / resource lifetime | failure behavior | exact source | T03 question |
|---|---|---|---|---|---|---|
| `LF-01` | Server startup and configuration | Composition root loads and validates one `repository_root`, non-empty explicit current roots, and optional legacy roots; invalid mandatory configuration prevents server start | Validated configuration is at least server-lifetime input; exact runtime representation and reload policy are unspecified | Missing, unreadable, outside-root, duplicate, inconsistent, or overlapping configured roots fail startup as a whole | `spec:drmcp.design_records_mcp.namespace_scanning`; `spec:drmcp.implementation` | Whether configuration is loaded once into immutable application state or accessed/reloaded through a per-use-case port, because both claims currently appear. |
| `LF-02` | Server run and shutdown | Composition root is claimed to own construction, wiring, run, and shutdown only | Server-owned transports and resources; no explicit shutdown ordering or retained-state flush behavior | Shutdown failure behavior is unspecified | `spec:drmcp.implementation`; `spec:drmcp.design_records_mcp.responsibility_boundary` | Which resources require shutdown hooks, especially MCP transport, proposal/cache stores, and in-flight writes. |
| `LF-03` | Read-operation request snapshot | Every invocation of `list_records`, `get_records`, `resolve_reference`, or `validate_records` rebuilds one fresh immutable snapshot and uses it from start to finish | Request-scoped; discarded after operation | Post-start scan, read, or index failure fails the current operation; no partial normal response from an untrustworthy snapshot | `spec:drmcp.design_records_mcp.namespace_scanning`; `spec:drmcp.implementation` | Shared snapshot builder versus operation-specific orchestration, and consistency behavior when files change during enumeration/read. |
| `LF-04` | Current-source discovery and active-index construction | Enumerate configured current roots, retain source provenance, parse source and metadata, derive identity, retain invalid state, build unique entries and conflict groups with no duplicate winner | Request-scoped inside W011 snapshot; current records only | Invalid individual records remain addressable or validation-only; invalid root fails operation/startup; duplicate identity removes only that identity's winner | `spec:drmcp.design_records_mcp.namespace_scanning`; `schema.discovery`; `schema.record_model`; `spec:drmcp.implementation` | Boundary among source access, parsing, identity, index construction, and snapshot orchestration. |
| `LF-05` | Legacy lookup-map construction | Optional configured legacy roots are validated separately, scanned recursively under strict alias boundaries, exact issued IDs are derived from filenames, and unique/absent/unreadable/conflicted lookup states are retained | Request-scoped for four W011 operations; disabled when roots absent/empty | Invalid configured root fails startup/operation; invalid candidate may be excluded; duplicate ID has no winner; unrelated entries remain | `spec:drmcp.design_records_mcp.namespace_scanning`; `spec:drmcp.implementation` | Whether legacy compatibility is an adapter capability, separate component, or policy inside indexing, while preserving state-family separation. |
| `LF-06` | Validation execution | Enumeration/read/parse/index complete first; per-source detectors, current relations, legacy relations, Topics graph, aggregation/dedup/order/projection follow | Request-scoped findings and projection; validators are stateless/pure with no I/O | Any missing required input/location fails execution; invalid content becomes findings data | `spec:drmcp.design_records_mcp.tools.validate_records`; `schema.diagnostics`; `spec:drmcp.implementation` | Reusable validation engine boundary and how proposal-local validation invokes the same rules without calling public tools. |
| `LF-07` | Guidance discovery and retrieval | Discover `docs/guides/*.md`, derive IDs, parse title/abstract, or return full verbatim content | No cache, snapshot, startup-load, or freshness lifetime specified | Missing/unreadable/malformed source behavior mostly unspecified | `schema.authoring_guidance_source`; `tools.list_authoring_guides`; `tools.get_authoring_guidance` | Guidance catalog lifecycle, source adapter, and failure ownership. |
| `LF-08` | Proposal preparation and retention | Create/update resolves target, constructs candidate state and diff, validates affected set, then retains a `proposed` object; repository files remain unchanged | Proposal retention: 3 days; `base_state` retained for accept-time staleness checks | Error diagnostics or required-location failure prevent proposal retention; no repository write | `schema.authoring_transaction_schema`; `tools.authoring_transaction_model`; create/update contracts | Proposal store implementation boundary, candidate-state representation, expiry cleanup, and process-restart behavior. |
| `LF-09` | Body-cache retry | Submitted large section body may be stored as an opaque body-cache object after preparation failure and reused for retries | Reusable for 3 days, including after proposal creation | Unknown/expired cache blocks proposal; body plus cache together invalid; cache-only create invalid | `schema.authoring_transaction_schema`; `tools.authoring_transaction_model`; create/update contracts | Body-cache ownership, storage, cleanup, size/resource limits, and restart semantics. |
| `LF-10` | Accept-time revalidation and write | Reload proposal, check lifecycle/base/target/ID/follow-up/validation/location invariants, then write files and transition result | One accept invocation; proposal becomes accepted on success; written files persist | All pre-write failures produce no writes; after actual modifications the result must preserve actual written state; rollback is not automatic | `tools.authoring_transaction_model`; `tools.accept_proposed_write` | Serialization, locking, all-or-none behavior for multi-file proposals, crash recovery, and authoritative ordering of write versus state transition. |
| `LF-11` | Post-write validation and repair | Current-format post-write integration is explicitly deferred; if validation runs and fails after writing, report `written: true` and repair guidance | Persisted repository state outlives request; candidate/pre-write state must not substitute for persisted-source validation | No automatic rollback; caller creates repair proposal | `tools.accept_proposed_write`; `tools.authoring_transaction_model`; `spec:drmcp.implementation` | Whether post-write validation is part of accept, a separate orchestration stage, or a future use case, and which snapshot is authoritative. |
| `LF-12` | Proposal discard and expiry | Discard changes state to `discarded`; expiry makes proposal unavailable as a retained target | Proposal/cache TTL is 3 days; cleanup mechanism unspecified | Discarded cannot be accepted; accepted is not undone; unknown/expired returns diagnostics | `tools.discard_proposed_write`; `tools.get_proposed_write`; `tools.authoring_transaction_model` | State machine owner, expiry scheduler/lazy cleanup, and accept/discard race policy. |
| `LF-13` | Filesystem freshness | For W011 reads, filesystem changes become visible on the next invocation; no mutable process-wide index, incremental patch, watcher, background refresh, or read cache | Request-scoped fresh state only | In-request changes are not specified beyond fail-on-untrustworthy-snapshot language | `spec:drmcp.design_records_mcp.namespace_scanning`; `spec:drmcp.implementation` | Snapshot consistency boundary under concurrent external writes and whether authoring acceptance invalidates or bypasses any future cache. |

### External-boundary inventory

| boundary | observed contract | data / operation crossing boundary | failure or policy ownership | exact source | T03 input |
|---|---|---|---|---|---|
| MCP transport / host | Inbound adapter for four W011 operations owns protocol schema, decode, structural JSON validation, application invocation, and encoding; other public tool mappings are not architected | Public requests, results, diagnostics, and execution errors | MCP adapter preserves application semantic-vs-execution classification; unknown retired tools belong to host boundary | `spec:drmcp.implementation`; `tools.overview`; `responsibility_boundary` | Define one complete inbound boundary for all current public tools without letting protocol types enter application/core. |
| Current filesystem records | Concrete enumeration and reading are outer-adapter responsibilities through narrow application-owned ports | Configured current Markdown sources, source identity, bytes, repository-relative locations | Root validity, source unreadability, alias escape, and required-location failures are fail-closed at their owning operation | `namespace_scanning`; `schema.discovery`; `spec:drmcp.implementation` | Determine source-access capability boundaries and whether writer is the same adapter family as reader. |
| Legacy filesystem archives | Separate configured read-only roots and exact lookup map; no auto-discovery and no authoring | Legacy Markdown files and issued IDs | Root invalidity fails startup; candidate invalidity may exclude candidate; read operations retain distinct lookup causes | `namespace_scanning`; `resolver`; read tool contracts | Preserve compatibility isolation and prohibit current/legacy merged repository semantics. |
| Repository writer | Only `accept_proposed_write` may modify repository files; request paths are never primary identity | Candidate diffs and one or more affected current files | Pre-write checks prevent writes; actual writes must be reported; atomicity and crash behavior unspecified | `tools.authoring_transaction_model`; `tools.accept_proposed_write` | Define explicit write port, transaction coordinator, and state transition boundary. |
| Runtime configuration source | Concrete loading belongs to configuration adapter; semantic root configuration is required at startup; W011 use cases also depend on configuration access | Repository root, current roots, legacy roots | Invalid mandatory configuration prevents startup and can prevent operation snapshot construction | `namespace_scanning`; `spec:drmcp.implementation` | Reconcile startup-loaded state with per-request configuration access. |
| PRODUCT standards / semantic authorities | DRMCP consumes PRODUCT identity, layout, spec-format, traceability, authoring, and lifecycle rules but does not own semantic validity | Rule vocabularies, required fields/sections, relation rules, ID/ref grammar, placement rules | PRODUCT owns semantics; DRMCP owns parsing, application of rules, findings, and operation representation | `overview`; `schema.diagnostics`; `tools.validate_records`; `responsibility_boundary` | Runtime rule-source form is absent: compiled rules, injected catalog, or filesystem-loaded standards package must be decided or explicitly excluded. |
| Authoring guidance package | Separate read-only `docs/guides/*.md` source, not Design Records and not resolver targets | Guide Markdown, IDs, title, abstract, full content | Only guide-not-found is explicit; source availability/freshness failure is unspecified | `schema.authoring_guidance_source`; guidance tool contracts | Define its configuration, source port, snapshot/cache, and error boundary. |
| Proposal store | Retains proposal, diff, target, base state, validation, lifecycle, and expiry | Opaque proposal IDs and retained write candidates | Proposal tools own lifecycle outcomes; concrete storage and durability are unspecified | `schema.authoring_transaction_schema`; `tools.authoring_transaction_model` | Decide storage boundary, concurrency, durability, cleanup, and ownership. |
| Body-cache store | Retains large section bodies for retry by opaque ID | Body bytes/content and expiry | Unknown/expired/invalid combinations block proposal | same authoring sources | Decide storage limits, durability, cleanup, and sharing with proposal store. |
| Existing brewprint MCP | Separate process/data source over `ResolvedProject`; DRMCP does not extend its `QueryService` | No runtime collaboration is specified | Product-level responsibility boundary | `overview`; `responsibility_boundary` | Treat as external and independent unless a later requirement introduces collaboration. |
| Model provider | No scoped Specification defines model inference, model-provider I/O, or provider-owned state | None observed | Unspecified | No source in scoped trees | T03 must explicitly classify model provider as outside current architecture or open a bounded follow-up; it must not be invented as an existing dependency. |

### State and resource inventory

| state / resource | observed owner claim | lifetime and mutation | concurrency / freshness constraint | exact source | unresolved architecture input |
|---|---|---|---|---|---|
| Startup configuration | Composition root loads/validates; configuration adapter performs concrete loading | Server lifetime is implied; mutation/reload unspecified | All configured roots mandatory; no partial startup | `namespace_scanning`; `spec:drmcp.implementation` | Canonical owner and reload semantics. |
| Read request snapshot | Application use case / shared snapshot orchestration | Fresh, immutable, one invocation only | Filesystem changes visible next invocation; no shared mutable index | `namespace_scanning`; `spec:drmcp.implementation` | Shared snapshot service ownership and consistency detection. |
| Source state | Core model, populated through source ports/adapters | Request-scoped in W011; raw bytes, identity, provenance | Must retain invalid/unreadable provenance sufficient for diagnostics | `schema.record_model`; `spec:drmcp.implementation` | Source identity model shared across current, legacy, guidance, and authoring. |
| Parsed state | Core records/parsers | Request-scoped for W011; missing stays missing, invalid present values retained | No default/repair; parser performs no I/O where practical | `schema.record_model`; `schema.fields`; `spec:drmcp.implementation` | Parser/rule boundary and reuse by authoring. |
| Current active index | Core indexing / snapshot | Fresh request-scoped immutable map; current records only | Duplicate identity has no winner; unrelated entries remain usable | `namespace_scanning`; `schema.record_model`; `spec:drmcp.implementation` | Whether one index capability serves read, authoring target resolution, and validation candidate state. |
| Current identity conflict | Core indexing state | Request-scoped group retaining all sources | Deterministic no-winner behavior; complete location collection required | same current-index sources; `schema.diagnostics` | Conflict model ownership and projection. |
| Invalid-but-addressable record | Core record/index state | Request-scoped | Canonical ID remains addressable; missing and invalid fields preserved distinctly | `schema.discovery`; `schema.record_model` | Avoid nullable record spanning layers while enabling operation-specific projections. |
| Validation-only source | Core source/parsed state | Request-scoped | Selectable only by repository/app validation, not exact ref | `schema.discovery`; `tools.validate_records` | Validation subject store versus active index. |
| Legacy lookup state | Separate core indexing state | Fresh request-scoped map for W011; read-only | Exact ID to resolved/absent/unreadable/conflicted; never merged with current index | `namespace_scanning`; `spec:drmcp.implementation` | Dedicated compatibility capability and adapter boundary. |
| Validation finding | Core validator output, aggregated by application | Invocation-local immutable data | Deterministic severity, semantic dedup, ordering; no I/O in validators | `schema.diagnostics`; `spec:drmcp.implementation` | Finding pipeline shared by standalone and proposal validation. |
| Operation projection | Dedicated application use case | Invocation-local | Core must not build complete tool responses; MCP only encodes | `spec:drmcp.implementation`; `tools.overview` | Projection ownership for all non-W011 operations. |
| Guidance catalog/content | No architecture owner stated | Freshness, caching, and lifetime unspecified | ASCII ordering for catalog; verbatim content for get | guidance schema/tools | Define catalog state or direct source access. |
| Proposal | Authoring transaction contract | Retained mutable lifecycle object for 3 days | Accept/discard/get races and restart durability unspecified | `schema.authoring_transaction_schema`; `tools.authoring_transaction_model` | State store and transition coordinator. |
| Proposal `base_state` | Authoring transaction contract | Retained until expiry/terminal state | Detects stale target, changed target, and ID collision; concrete hash/timestamp/snapshot unspecified | same authoring sources | Define comparable repository version token and race-free check/write boundary. |
| Candidate repository state / affected set | Proposal validation flow | Ephemeral during propose and accept checks | Validation limited to files actually created/modified; must use same rules as standalone validation | `tools.authoring_transaction_model`; `schema.diagnostics` | Candidate overlay model and validation invocation boundary. |
| Body cache | Authoring transaction contract | Reusable mutable/retained object for 3 days | Process sharing, size limit, cleanup, and durability unspecified | `schema.authoring_transaction_schema`; `tools.authoring_transaction_model` | Cache store ownership and resource limits. |
| Diff | Proposal-owned transparency output and retained content | Retained with proposal; summary/patch/none changes response only, not retained semantics | Paths must be normalized repository-relative and consistent across diff/write outputs | `tools.authoring_transaction_model` | Diff generator and persisted representation ownership. |
| Written file set | Accept operation | Persistent repository mutation; `files_written` reflects actual modifications | Pre-write no-op on guard failure; multi-file atomicity and partial crash state unspecified | `tools.accept_proposed_write` | Transaction/write coordinator and recovery model. |
| Diagnostic source location | Shared diagnostics contract | Invocation/proposal-local projection of repository-relative provenance | Required location absence fails operation; no absolute path exposure | `schema.diagnostics` | Single location builder capability and failure propagation owner. |

### Cross-cutting architecture constraints

| ID | observed constraint | exact source |
|---|---|---|
| `CC-01` | Canonical IDs/refs and exact tool inputs are never trimmed, case-repaired, prefix-completed, path-inferred, fuzzily matched, or alias-resolved unless an explicit contract says so. | `schema.id_normalization`; `resolver`; `tools.get_records`; `tools.resolve_reference` |
| `CC-02` | Current active index and legacy lookup state remain separate; no merged generic index may hide source-family behavior. | `namespace_scanning`; `spec:drmcp.implementation` |
| `CC-03` | Each W011 read invocation uses one fresh immutable snapshot; no process-wide mutable index, incremental patch, watcher, background refresh, or index cache. | `namespace_scanning`; `spec:drmcp.implementation` |
| `CC-04` | An untrustworthy mandatory snapshot, incomplete conflict collection, or missing required diagnostic location is an execution failure, not partial normal data. | `schema.diagnostics`; `tools.validate_records`; `spec:drmcp.implementation` |
| `CC-05` | Partial-success semantics are operation-specific: exact retrieval preserves successful refs; resolver negative statuses are normal results; validation never returns a partial diagnostic collection; proposal preparation never writes. | operation contracts and `spec:drmcp.implementation` |
| `CC-06` | Normal successful list, retrieval, and resolver projections hide source paths and provenance. Diagnostics and authoring transaction outputs expose only normalized repository-relative locations/paths under their own contracts; absolute paths are prohibited. | `schema.diagnostics`; list/get/resolve contracts; `tools.authoring_transaction_model` |
| `CC-07` | For W011 operations, application use cases own typed transport-neutral inputs/outputs, sequencing, projection, and request-snapshot orchestration; core returns domain values/findings, and MCP owns protocol mapping only. | `spec:drmcp.implementation`; `tools.overview` |
| `CC-08` | No generic App Router and no all-purpose repository interface are accepted for the four W011 operations. | `spec:drmcp.implementation`; `responsibility_boundary` |
| `CC-09` | Validators perform no filesystem I/O, do not rescan roots, call public MCP tools, invoke validators through transport, or format MCP responses. | `tools.validate_records`; `spec:drmcp.implementation` |
| `CC-10` | Diagnostic categories, associations, severity, semantic duplicate suppression, and ordering are deterministic and shared across standalone and proposal-local validation. | `schema.diagnostics` |
| `CC-11` | PRODUCT owns semantic validity, identity, layout, traceability, authoring, and lifecycle rules; DRMCP owns consumption, parsing, indexing, operation policy, concrete diagnostics, and transport representation. | `overview`; `responsibility_boundary`; `schema.diagnostics`; `tools.validate_records` |
| `CC-12` | Proposal creation never writes. Only `accept_proposed_write` may write repository files. Pre-write error diagnostics and guard failures prevent all writes. | `tools.authoring_transaction_model`; create/update/accept contracts |
| `CC-13` | Proposal-local validation is limited to the affected record set in candidate state and must be reproducible by the same validation rules. Unrelated repository errors do not block the proposal. | `schema.authoring_transaction_schema`; `tools.authoring_transaction_model`; `schema.diagnostics` |
| `CC-14` | Proposal and body-cache retention is 3 days. Expired objects are not valid operation targets. | authoring schema/model and proposal tools |
| `CC-15` | Retired `get_record` and `suggest_next_record` must be omitted from the server catalog and must not receive compatibility routes. | retired tool contracts; `tools.overview` |
| `CC-16` | Core must not depend on application, MCP, filesystem, or configuration packages in the W011 architecture; adapters depend inward and composition performs wiring. | `spec:drmcp.implementation` |
| `CC-17` | Package cycles are prohibited in the W011 package layout, but that layout is explicitly not the complete server architecture. | `spec:drmcp.implementation` |
| `CC-18` | External filesystem modifications become visible to W011 reads on the next invocation. Concurrent mutation during snapshot construction has no more precise contract. | `namespace_scanning`; `spec:drmcp.implementation` |

### Existing ownership claims

These are current Specification claims, not accepted whole-application architecture decisions.

| claimed owner | existing responsibility claim | explicit scope limit / exclusion | exact source |
|---|---|---|---|
| Composition root | Startup configuration, construction, wiring, run, shutdown | Owns no parsing, indexing, resolution, validation, or operation policy; current claim is W011-only | `spec:drmcp.implementation`; `responsibility_boundary` |
| MCP inbound adapter | Protocol schemas, decode, structural validation, invocation, encoding | Explicitly architected only for `list_records`, `get_records`, `resolve_reference`, and `validate_records`; no SDK types inward | same sources |
| Dedicated W011 application use cases | Typed input/output, operation sequencing, projection, request snapshot | Does not cover guidance or authoring operations; no generic App Router | `spec:drmcp.implementation`; `tools.overview` |
| `internal/app/snapshot` claim | Shared snapshot orchestration and application-owned configuration/source ports | Package-level claim limited to four read operations | `spec:drmcp.implementation` |
| Core records/index/resolving/validation | Transport-neutral parsing, indexing, resolver logic, validators, and finding values | Must not construct complete tool responses or perform external I/O | `spec:drmcp.implementation`; `schema.diagnostics` |
| Filesystem/config adapters | Concrete source enumeration, reading, and configuration loading through narrow application-owned ports | Outbound adapters do not invoke application use cases | `spec:drmcp.implementation` |
| W003 contract area | Current-root discovery, parsing, canonical identity, active-index construction, normalized record state, invalid-source retention, conflicts | Public operation behavior belongs elsewhere | `responsibility_boundary`; operation Boundary tables |
| W004 contract area | Listing, exact retrieval, ordering, partial success, body inclusion, normal projection | Does not own resolver orchestration, shared diagnostic representation, or validation execution | `responsibility_boundary`; list/get contracts |
| W005 contract area | Resolver current-first orchestration and configured legacy fallback | Does not own exact retrieval or diagnostics representation | `responsibility_boundary`; resolver contracts |
| W006 contract area | Validation execution and shared warning/diagnostic representation, location, path policy | PRODUCT retains semantic validity | `responsibility_boundary`; `schema.diagnostics`; validate contract |
| Authoring transaction contracts | Proposal/cache lifecycle, candidate diff, proposal validation affected set, write eligibility and write result | Explicitly outside `spec:drmcp.implementation`; post-write current-format validation integration deferred | authoring schema/model and accept contract |
| Authoring guidance contracts | Guide source discovery and public list/get projections | Explicitly outside W011 application architecture | guidance schema/tools; `tools.overview` |
| PRODUCT Design Records Specifications | Semantic identities, placement, authoring, document shape, relation and lifecycle validity | DRMCP owns concrete runtime consumption and response behavior | `overview`; `responsibility_boundary`; diagnostics/validate contracts |

### Gaps, conflicts, and stale assumptions

| ID | classification | observed gap, conflict, or stale representation | exact evidence | architecture implication for T03 only |
|---|---|---|---|---|
| `G-01` | Scope gap | `spec:drmcp.implementation` covers only four W011 read operations and explicitly excludes guidance and authoring operations, while the public catalog contains eleven current operations. | `spec:drmcp.implementation`; `tools.overview` | Whole-application architecture must cover all current operation families rather than extending the read slice by assumption. |
| `G-02` | Stale read-runtime assumption | The current implementation Specification contains accepted layers, packages, dependencies, and snapshot ownership at package granularity but states that it is not the complete server architecture. | `spec:drmcp.implementation`; `overview` | T03 must treat these as existing claims/candidates and test them against all use cases, not adopt them as the application graph. |
| `G-03` | Direct Specification conflict | Current spec source contracts require H1-adjacent visible metadata and reject YAML front matter, but authoring transaction schema/update contracts still define spec metadata replacement inside YAML front matter with legacy recognized fields. | `schema.record_source`; `schema.metadata_grammar`; `schema.discovery` versus `schema.authoring_transaction_schema`; `tools.propose_record_update` | Authoring cannot share the current parser/validator/write architecture coherently until the canonical current-format mutation model is selected by downstream design/authoring work. |
| `G-04` | Stale identity and placement representation | Authoring create/update contracts use `V01-*`, `v01/records/**`, legacy namespace-prefix language, and `V01-SPEC-*`, while current discovery/identity authority uses app-aware IDs, active app roots, and path-derived `spec:` refs; `v01/records` is a read-only archive under startup authority. | create/update contracts versus `overview`, `namespace_scanning`, `schema.id_normalization`, and `prompt_chappy.md` | Current authoring target resolution, placement discovery, and sequential allocation are not coherently specified. |
| `G-05` | Direct authority conflict | Scoped DRMCP Task schema requires `source_requirement` and omits `task_type` and `work_item_ref`; current Task-authoring authority prohibits Task source fields, requires `task_type`, and conditionally requires `work_item_ref`. | `schema.record_source`; `schema.fields`; `schema.metadata_grammar` versus `spec:product.design_records.authoring_standards.task_authoring` | Parser, validator, projection, and authoring field catalogs cannot be assumed correct for current Tasks; T03 should isolate rule consumption from stale field copies. |
| `G-06` | Architecture gap | Authoring-guidance operations define a source and projections but no startup, configuration, snapshot, cache, malformed-source, unreadable-source, or package ownership model. | guidance schema/tools; `tools.overview` | Guidance needs an explicit application/source/lifecycle boundary or an explicit exclusion from the first architecture baseline. |
| `G-07` | Architecture gap | Proposal and body-cache operational objects have TTL and public lifecycle but no storage backend, durability, restart, cleanup, resource-limit, or corruption behavior. | authoring transaction schema/model | State/resource ownership cannot be assigned from current specs. |
| `G-08` | Concurrency and transaction gap | Multi-file proposals are possible, accept performs writes, and actual writes must be reported, but lock scope, all-or-none semantics, write ordering, crash recovery, and concurrent accept/discard or accept/accept outcomes are undefined. | create contract; authoring model; accept/discard contracts | A write-transaction and proposal-state coordination decision is required before contract/detail design. |
| `G-09` | Lifecycle ambiguity | Composition root loads and validates configuration at startup, while W011 application use cases also depend on a configuration-access port; whether the port returns immutable validated state or reloads external configuration is not stated. | `namespace_scanning`; `spec:drmcp.implementation` | Configuration ownership and lifetime require one coherent architecture decision. |
| `G-10` | Freshness gap | W011 snapshots are fresh and immutable and changes appear next invocation, but no contract defines detection or handling of filesystem mutation between enumeration and reads during one snapshot build. | `namespace_scanning`; `spec:drmcp.implementation` | Snapshot consistency and source-version checks remain open. |
| `G-11` | Standards boundary gap | Validation and authoring consume many PRODUCT semantic authorities, but no runtime standards package, rule-catalog loading, versioning, or dependency boundary is defined. | diagnostics/validate/overview authority tables | T03 must decide whether rules are compiled application dependencies, injected data, or another external source; it must not invent model inference as rule evaluation. |
| `G-12` | External-boundary absence | No scoped Specification mentions a model provider or model inference. | Full scoped-tree review | Model provider must be explicitly excluded or routed to separate investigation; it is not an observed DRMCP dependency. |
| `G-13` | Lifecycle gap | Shutdown is assigned to composition root but no resource shutdown order, in-flight request drain, store flush, or interrupted-write behavior is defined. | `spec:drmcp.implementation` | Whole-application resource lifecycle remains incomplete. |
| `G-14` | Deferred integration | Current-format post-write validation integration is deferred, and authoring contracts explicitly do not require use of the W011 snapshot after writing. | `tools.authoring_transaction_model`; `tools.accept_proposed_write`; `spec:drmcp.implementation` | Shared validation/snapshot reuse across read and authoring is an unresolved architecture boundary. |
| `G-15` | Capability coverage gap | Create supports decision, requirement, work item, and task only; investigation and spec create are outside MVP. Update supports spec but under stale YAML/V01 semantics. | create/update contracts | T03 must distinguish current application architecture from future/non-MVP capability without creating phantom use cases. |
| `G-16` | Failure-contract gap | Several operations state that missing required locations or untrustworthy state cause execution failure but intentionally assign no concrete error identifier; guide-source and retained-store I/O failures are also underspecified. | list/get/resolve/validate contracts; guidance/proposal contracts | Failure ownership can be placed at architecture level, but exact error contracts require later specification work. |
| `G-17` | State-transition ambiguity | Accept says `state: accepted` on successful write and insists actual post-write modification cannot be reported as `written: false`, but does not fully define terminal proposal state after partial or failed multi-file modification. | `tools.accept_proposed_write` | Proposal lifecycle and write-result state machine require explicit coordination. |
| `G-18` | Semantic vocabulary gap | PRODUCT has no complete allowed-value set for spec `status`, while list filtering and validation rely on PRODUCT-owned vocabularies. | `schema.fields`; list/validate contracts | Rule catalogs and operation behavior must tolerate incomplete external semantic authority without inventing values. |
| `G-19` | Security/non-functional gap | Public write operations, guide access, and repository reads define no authentication, authorization, sandboxing policy beyond path confinement, request size limits beyond selected fields, or audit behavior. | Full scoped-tree review; path constraints in diagnostics/authoring model | T03 should classify these as explicit non-goals, external host responsibilities, or follow-up inputs rather than silently assigning them. |

### Inputs for T016-03

T03 can initialize its decision ledger directly from these unresolved architecture inputs. The items below are questions, not decisions.

| decision-input ID | required judgment | Evidence that makes it necessary | minimum use-case coverage |
|---|---|---|---|
| `AI-01` | Define the whole DRMCP application boundary and explicitly classify current public operations, retired tombstones, existing brewprint MCP, model provider, PRODUCT standards, and host runtime. | `UC-R01` through `UC-A05`, `UC-X01`/`UC-X02`, external-boundary inventory, `G-01`, `G-11`, `G-12` | All families |
| `AI-02` | Select the minimum stable application-level component graph that covers read/navigation, guidance, authoring proposal, retained state, write acceptance, and shared parsing/indexing/validation capabilities. | Use-case inventory and `G-01`/`G-02` | All current public operations |
| `AI-03` | Assign owned and excluded responsibilities for source access, parsing, identity, indexing, lookup, rule evaluation, validation, projection, diff/rendering, proposal state, and filesystem writing. | ownership claims, state inventory, `G-03` through `G-08` | Read + authoring + guidance |
| `AI-04` | Decide allowed dependency directions and forbidden dependencies while preserving transport neutrality, PRODUCT semantic authority, separate current/legacy state, and no public-tool recursion. | `CC-01` through `CC-17`; existing W011 claims | All families |
| `AI-05` | Decide runtime collaboration for each materially distinct operation family and identify which shared stages are reusable without collapsing operation-specific policies. | `UC-R01`–`UC-R04`, `UC-G01`–`UC-G02`, `UC-A01`–`UC-A05`; `LF-03`–`LF-12` | Each family separately |
| `AI-06` | Decide configuration ownership, startup validation, immutable runtime configuration, reload behavior, and shutdown order. | `LF-01`, `LF-02`, `G-09`, `G-13` | Server lifecycle |
| `AI-07` | Decide snapshot/source freshness semantics and ownership for reads, validation, proposal preparation, and post-write persisted-state validation. | `LF-03`, `LF-06`, `LF-13`, `G-10`, `G-14` | Read + validate + authoring |
| `AI-08` | Decide current and legacy source adapters/index capabilities while preserving no-merge and no-winner constraints. | `LF-04`, `LF-05`, `CC-02`, `CC-03` | Retrieval + resolver + validation |
| `AI-09` | Decide the runtime boundary for PRODUCT standards/rule catalogs and how stale DRMCP-local field copies are prevented from becoming semantic authority. | `G-05`, `G-11`, `G-18` | Validation + authoring + filtering |
| `AI-10` | Decide guidance source configuration, loading/freshness, malformed/unreadable behavior, and application ownership. | `UC-G01`, `UC-G02`, `LF-07`, `G-06` | Guidance operations |
| `AI-11` | Decide proposal and body-cache state ownership, persistence, TTL cleanup, restart behavior, resource limits, and concurrency serialization. | `LF-08`, `LF-09`, `LF-12`, state inventory, `G-07`, `G-17` | Proposal get/discard/create/update/accept |
| `AI-12` | Decide write transaction, lock, multi-file atomicity, crash recovery, actual-write reporting, and post-write validation/repair ownership. | `UC-A04`, `LF-10`, `LF-11`, `G-08`, `G-14`, `G-17` | Accept and reciprocal updates |
| `AI-13` | Resolve or quarantine stale current-format/V01/YAML authoring contracts before using them as architecture authority. | `G-03`, `G-04`, `G-05`, `G-15` | Authoring operations |
| `AI-14` | Decide failure ownership and semantic-result versus execution-error boundaries across operation families, including required-location failure and underspecified source/store failures. | Use-case failure columns, `CC-04`, `CC-05`, `G-16` | All families |
| `AI-15` | Define which downstream contract/detail changes remain local and which discoveries reopen application architecture, especially new external systems, new shared mutable state, changed snapshot/write semantics, or component-crossing policy. | W016 completion contract plus all gaps above | Downstream handoff |
| `AI-16` | Decide the canonical architecture views and provisional ADR boundaries only after `AI-01` through `AI-15` converge. | `DRMCP-TASK-MCP-016-03` Goal and W016 completion contract | Canonical projection planning |

Suggested first decision order for T03, without adopting an answer:

1. `AI-01` application boundary and operation-family scope.
2. `AI-13` authority quarantine for stale authoring representations, because unresolved source authority would contaminate component selection.
3. `AI-06` and `AI-07` lifecycle/snapshot baseline.
4. `AI-02` and `AI-03` component graph and responsibility ownership.
5. `AI-04` dependency and forbidden-dependency model.
6. `AI-05`, `AI-08`, `AI-09`, and `AI-10` runtime collaborations and external-source boundaries.
7. `AI-11` and `AI-12` retained state and write transaction.
8. `AI-14` failure ownership, then `AI-15` downstream return rules and `AI-16` projection/ADR inputs.

### Verification result

- Both scoped Specification trees were completely enumerated and read: 31 files total.
- Every operation in the current public catalog was inventoried: four read-runtime operations, two authoring-guidance operations, and five authoring-transaction operations.
- `get_record` and `suggest_next_record` were explicitly classified as retired and not treated as active architecture use cases.
- Startup, shutdown, configuration, composition claims, request snapshots, current and legacy index state, validation state, proposal state, body cache, write state, external boundaries, resource lifetime, freshness, failure ownership, and stale assumptions were inventoried separately from public use cases.
- Observed Specification facts, cross-Specification gaps/conflicts, and undecided T03 architecture inputs are separated.
- No separate Investigation record was created.
- The post-Evidence semantic responsibility validator could not be invoked: `spec:product.responsibility_boundary_validator` assigns the application to TRV and excludes current DRMCP integration, while the only repository prompt asset is explicitly deprecated and prohibited as an automated validator or workflow gate. No semantic result was synthesized.
- No application component, component graph, responsibility boundary, dependency direction, forbidden dependency, architecture style, canonical architecture view, ADR route, or Specification change was selected or authored.
- No Work Item, Requirement, Task graph, T016-03 ledger, implementation, test, fixture, review, closure, stage, or commit work was performed.
- Scoped Git inspection reports this Task as `untracked` and `unstaged`. The complete scoped patch is available, whitespace inspection passes with no findings, and the only advisory is LF-to-CRLF conversion.
- This Task is the only file intentionally modified, and its lifecycle status is now `done`.
