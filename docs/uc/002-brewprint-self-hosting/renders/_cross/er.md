# mcp_contract_er

UC-002 Phase A MCP公開contractのER view。
MCP schema model群とResolvedProject context storeを含むmcp moduleを対象にする。
ただしv1 ER rendererはDB store由来のentityのみを描画するため、Phase Aのcontract schema modelはcoverage.mdで確認する。

```mermaid
erDiagram
```
