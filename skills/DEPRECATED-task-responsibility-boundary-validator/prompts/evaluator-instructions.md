Evaluate every supplied criterion. Do not add, omit, or reorder criteria.
Use only information written in the Task.
If the Task does not contain enough information to confirm a criterion, return false.
For each criterion, return its exact ID, a boolean result, a short reason, and the relevant Task section.
Return only a JSON array with id, result, reason, and section fields.
