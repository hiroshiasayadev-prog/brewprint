[project]
name = "brewprint-usdm-mcp"
version = "0.1.0"
description = "MCP wrapper for Brewprint MVP USDM validation and coverage tools"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.27,<2",
    "mcp-proxy>=0.12,<1",
]

[tool.uv]
package = false
