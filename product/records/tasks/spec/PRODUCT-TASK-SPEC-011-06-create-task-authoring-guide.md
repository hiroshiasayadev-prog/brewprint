# PRODUCT-TASK-SPEC-011-06: Create task authoring guide

- **id**: PRODUCT-TASK-SPEC-011-06
- **status**: done
- **date**: 2026-06-23
- **work_item**: PRODUCT-WORK-SPEC-011
- **source_requirement**: PRODUCT-REQ-SPEC-002
- **estimate**: 0.5d
- **depends_on**:
  - PRODUCT-TASK-SPEC-011-02
  - PRODUCT-TASK-SPEC-011-03
  - PRODUCT-TASK-SPEC-011-05
- **outputs**:
  - `product/records/spec/concepts/authoring-standards/task-authoring.md`
  - `product/records/spec/concepts/authoring-standards/index.md`

## Goal

Create the canonical task authoring guide under PRODUCT authoring standards.

## Work

- Use the common per-artifact guide structure.
- Define namespace-aware task ID grammar and file layout.
- Define task sequence scope under the parent work item.
- Define canonical English body headings.
- Define task metadata meaning and requiredness for create, partial update, and persisted state.
- Define task status lifecycle.
- Define short-term work, verification, output, dependency, and evidence rules.
- Distinguish task execution state from work-item goal state and task graph ownership.
- Reference `artifact_boundary` for cross-artifact selection.
- Define author-facing exact-ID and parent-scoped `new` forms.
- Exclude concrete DRMCP request, response, and diagnostic schemas.
- Update the authoring-standards Index.

## Done condition

| item | done when |
|---|---|
| Guide published | `task-authoring.md` exists as a Reference spec. |
| Common shape applied | The guide follows PRODUCT-WORK-SPEC-011 section structure. |
| Parent-scoped ID defined | TASK sequence and `new` behavior are defined under the parent work item. |
| English headings canonical | All prescribed task headings use English. |
| Metadata states separated | Create, partial update, and persisted requirements are explicit. |
| Index updated | The guide appears with its canonical ref and current summary. |

## Verification

- Confirm the guide uses `<APP>-TASK-<DOMAIN>-<WORK-SEQUENCE>-<TASK-SEQUENCE>`.
- Confirm the author-facing placeholder uses `<APP>-TASK-<DOMAIN>-<WORK-SEQUENCE>-new`.
- Confirm task headings and table headers use English.
- Confirm verification and Evidence are distinct and required where appropriate.
- Confirm no current DRMCP operating status appears.

## Evidence

- `product/records/spec/concepts/authoring-standards/task-authoring.md` created as a Reference spec.
- Common guide shape from PRODUCT-WORK-SPEC-011 applied: all sections present in order.
- All prescribed headings use English.
- Task ID grammar defined as `<APP>-TASK-<DOMAIN>-<WORK-SEQUENCE>-<TASK-SEQUENCE>` with `<WORK-SEQUENCE>` inherited from parent Work Item (3-digit) and `<TASK-SEQUENCE>` scoped to parent (2-digit). Author-facing placeholder is `<APP>-TASK-<DOMAIN>-<WORK-SEQUENCE>-new`.
- Metadata schema distinguishes create input, partial update, and persisted state across all eight fields.
- `id` field: generated by MCP on create, not supplied by author as a metadata field.
- `work_item` create input: supplied as parent Work Item ID at the top level; MCP generates persisted `work_item` metadata — author does not supply it as a metadata field. Partial update: correction only.
- `work_item` correction rules extended: corrected parent must share the same app namespace, domain, and work sequence as the Task ID; cross-sequence moves require a new Task or explicit migration; correction must align old and new parent `tasks` and `source_requirement`.
- `source_requirement` correction-only with rule that corrected value must match the parent Work Item's `source_requirement`.
- Body section table separates heading presence (always) from substantive content (required when `done` for all five sections).
- `TBD` placeholder prohibition stated: `TBD` must not remain in any done-gated section when `status` is `done`.
- All five sections done-gated (Goal, Work, Done condition, Verification, Evidence) — consistent with v01 guide validation contract.
- Persisted workflow relation invariants stated: parent Work Item lists this Task in `tasks`; Task `source_requirement` matches parent Work Item's `source_requirement`.
- Task granularity guidance restored from v01 guide: SHOULD close in 0.5d–3d; split considerations added (exceeds 3d, multiple independent concerns, can't describe as single completion).
- `## Evidence` wording clarified: execution results and verification outcomes only; design decisions belong in an ADR, referenced from Evidence.
- No tooling-specific rationale for TBD presence; normative rationale used instead.
- Guide cites `spec:product.concepts.authoring_standards.artifact_boundary`; does not duplicate the ownership matrix.
- No DRMCP operating status recorded.
- `product/records/spec/concepts/authoring-standards/index.md` updated with new entry.
